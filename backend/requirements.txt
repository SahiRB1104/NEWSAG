# Production dependencies for the FastAPI service.  Transitive dependencies are
# intentionally resolved by pip instead of being pinned from a local machine.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.128.0
uvicorn[standard]==0.40.0
python-dotenv==1.2.1
python-multipart==0.0.21
pydantic==2.12.5
email-validator==2.3.0

# Persistence, cache, and external HTTP services
motor==3.7.1
redis==7.1.0
httpx==0.28.1
PyJWT[crypto]==2.11.0

# News processing and text-to-speech
beautifulsoup4==4.14.3
deep-translator==1.11.4
boto3==1.42.49

# CPU inference and the in-application model fine-tuning endpoints
torch==2.10.0+cpu
transformers==4.57.6
safetensors==0.7.0
numpy==2.4.1
scikit-learn==1.8.0
datasets==4.5.0
accelerate==1.12.0
